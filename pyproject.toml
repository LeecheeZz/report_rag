[project]
name = "report-rag"
version = "0.1.0"
description = "PDF report parsing, indexing, retrieval, reranking, and generation demo."
requires-python = ">=3.10"
readme = "README.md"

[tool.ruff]
line-length = 100

